<section class="container" style="padding:60px 0 80px;max-width:820px">
  <h1 style="font-size:clamp(40px,5vw,72px);letter-spacing:-2px;margin:0 0 24px">{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</section>

{% schema %}
{ "name": "Página", "settings": [] }
{% endschema %}
