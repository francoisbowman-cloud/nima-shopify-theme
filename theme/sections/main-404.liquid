<section class="container empty-state">
  <h1 style="font-size:clamp(48px,7vw,96px);letter-spacing:-3px;margin:0 0 12px;color:var(--text)">404</h1>
  <p>{{ 'general.not_found' | t }}</p>
  <a class="btn" href="{{ routes.root_url }}">{{ 'general.back_home' | t }} →</a>
</section>

{% schema %}
{ "name": "404", "settings": [] }
{% endschema %}
